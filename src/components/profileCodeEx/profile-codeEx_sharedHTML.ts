'use strict';

import { profileCodeEx_sharedHTML } from '../../interfaces/interfaces.js';

const profileCodeEx_sharedHTML = {
	shell: ``,
	codeEx: ``,
	slide: ``
};

profileCodeEx_sharedHTML.shell = /*html*/ `

`;

profileCodeEx_sharedHTML.codeEx = /*html*/ `

`;

profileCodeEx_sharedHTML.slide = /*html*/ `

`;

export { profileCodeEx_sharedHTML };
