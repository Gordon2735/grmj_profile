'use strict';

import { timeObject_sharedHTML } from '../../interfaces/interfaces.js';

const timeObject_sharedHTML: timeObject_sharedHTML = {
    shell: ``,
    time: ``
};

timeObject_sharedHTML.shell = `


`;

timeObject_sharedHTML.time = `


`;

export { timeObject_sharedHTML };
